<?xml version="1.0" encoding="utf-8" ?>
<toolkit:Popup
    x:Class="SmartGenealogy.Views.Popups.NewMultimediaPopupPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:fonticons="clr-namespace:SmartGenealogy.Resources.FontIcons"
    xmlns:handlers="clr-namespace:SmartGenealogy.Handlers"
    xmlns:localization="clr-namespace:SmartGenealogy.Resources.Translations"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:viewModel="clr-namespace:SmartGenealogy.ViewModels.Popups"
    x:DataType="viewModel:NewMultimediaPopupPageViewModel">

    <Grid
        Margin="0,100,0,0"
        HorizontalOptions="{OnIdiom Phone='Fill',
                                    Default='Center'}"
        VerticalOptions="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{OnIdiom Desktop='600', Default='*'}" />
        </Grid.ColumnDefinitions>

        <Border BackgroundColor="{DynamicResource MainWrapperBackgroundColor}">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="20" />
            </Border.StrokeShape>

            <Grid RowDefinitions="Auto,1,*,Auto">
                <Label
                    Margin="0,20"
                    FontSize="20"
                    HorizontalOptions="Center"
                    Style="{StaticResource Title2}"
                    Text="{localization:Translate LabelAddMultimedia}" />

                <Rectangle
                    Grid.Row="1"
                    Margin="20,0,20,10"
                    Fill="{DynamicResource BaseSeparatorColor}"
                    HeightRequest="1"
                    VerticalOptions="Fill" />

                <ScrollView
                    Grid.Row="2"
                    Margin="20,16"
                    BackgroundColor="Transparent"
                    VerticalScrollBarVisibility="Never">

                    <VerticalStackLayout>
                        <Grid
                            Margin="0,20"
                            ColumnDefinitions="*,Auto"
                            ColumnSpacing="12"
                            RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto,Auto"
                            RowSpacing="16">

                            <Border Padding="20,5" Background="{DynamicResource EntryAndEditorsBackgroundColor}">
                                <handlers:BorderlessEntry
                                    HeightRequest="42"
                                    IsReadOnly="True"
                                    Placeholder="File path"
                                    PlaceholderColor="{DynamicResource BaseLightTextColor}"
                                    Text="{Binding FilePath}"
                                    TextColor="{DynamicResource BaseTextColor}" />
                            </Border>

                            <Button
                                Grid.Column="1"
                                BackgroundColor="{DynamicResource PrimaryColor}"
                                Command="{Binding OpenFileCommand}"
                                CornerRadius="14"
                                FontAttributes="Bold"
                                FontFamily="FaRegular"
                                HeightRequest="42"
                                Text="{x:Static fonticons:FaRegular.FolderOpen}"
                                TextColor="{DynamicResource White}"
                                ToolTipProperties.Text="{localization:Translate ToolTipOpenFolder}" />

                            <Border
                                Grid.Row="1"
                                Grid.ColumnSpan="2"
                                Padding="20,5"
                                Background="{DynamicResource EntryAndEditorsBackgroundColor}">
                                <handlers:BorderlessEntry
                                    HeightRequest="42"
                                    Placeholder="Caption"
                                    PlaceholderColor="{DynamicResource BaseLightTextColor}"
                                    Text="{Binding Caption}"
                                    TextColor="{DynamicResource BaseTextColor}" />
                            </Border>

                            <Border
                                Grid.Row="2"
                                Grid.ColumnSpan="2"
                                Padding="20,5"
                                Background="{DynamicResource EntryAndEditorsBackgroundColor}">
                                <handlers:BorderlessEntry
                                    HeightRequest="42"
                                    Placeholder="Ref number"
                                    PlaceholderColor="{DynamicResource BaseLightTextColor}"
                                    Text="{Binding RefNumber}"
                                    TextColor="{DynamicResource BaseTextColor}" />
                            </Border>

                            <Border
                                Grid.Row="3"
                                Grid.ColumnSpan="2"
                                Padding="20,5"
                                Background="{DynamicResource EntryAndEditorsBackgroundColor}">
                                <handlers:BorderlessEntry
                                    HeightRequest="42"
                                    Placeholder="Date"
                                    PlaceholderColor="{DynamicResource BaseLightTextColor}"
                                    Text="{Binding Date}"
                                    TextColor="{DynamicResource BaseTextColor}" />
                            </Border>

                            <Border
                                Grid.Row="4"
                                Grid.ColumnSpan="2"
                                Padding="20,5"
                                Background="{DynamicResource EntryAndEditorsBackgroundColor}">
                                <handlers:BorderlessEntry
                                    HeightRequest="42"
                                    Placeholder="Sort Date"
                                    PlaceholderColor="{DynamicResource BaseLightTextColor}"
                                    Text="{Binding SortDate}"
                                    TextColor="{DynamicResource BaseTextColor}" />
                            </Border>

                            <Border
                                Grid.Row="5"
                                Grid.ColumnSpan="2"
                                Padding="20,5"
                                Background="{DynamicResource EntryAndEditorsBackgroundColor}">
                                <handlers:BorderlessEntry
                                    HeightRequest="42"
                                    Placeholder="Description"
                                    PlaceholderColor="{DynamicResource BaseLightTextColor}"
                                    Text="{Binding Description}"
                                    TextColor="{DynamicResource BaseTextColor}" />
                            </Border>

                        </Grid>

                        <Grid
                            Margin="0,20"
                            ColumnDefinitions="*,*"
                            RowDefinitions="Auto"
                            RowSpacing="16">

                            <Button
                                Margin="0,10"
                                BackgroundColor="{DynamicResource PrimaryColor}"
                                Command="{Binding AddMultimediaCommand}"
                                CornerRadius="14"
                                FontAttributes="Bold"
                                HeightRequest="50"
                                Text="{localization:Translate ButtonAddMultimedia}"
                                TextColor="{DynamicResource White}" />

                            <Button
                                Grid.Column="1"
                                Margin="0,10"
                                BackgroundColor="{DynamicResource PrimaryColor}"
                                Command="{Binding CancelCommand}"
                                CornerRadius="14"
                                FontAttributes="Bold"
                                HeightRequest="50"
                                Text="{localization:Translate ButtonCancel}"
                                TextColor="{DynamicResource White}" />
                        </Grid>
                    </VerticalStackLayout>
                </ScrollView>
            </Grid>
        </Border>
    </Grid>
</toolkit:Popup>